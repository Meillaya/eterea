# Import fixtures

Small sanitized bookmark exports used by service and parser guardrail tests.
These files intentionally replace the older ignored `src/legacy/` local exports
so fresh clones can run the test suite without private bookmark data.
